import React, { Fragment } from 'react';
import { NavBar } from '../components/Nav/NavBar';
const Dashboard = () => {
  return (
    <Fragment>
      <NavBar />
    </Fragment>
  );
};

export default Dashboard;
